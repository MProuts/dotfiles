development:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: "logcheck"
  pool: 5
  username: "logcheck"

  # Localhost
  # =========
  password: "logcheck"
  socket: /opt/local/var/run/mysql55/mysqld.sock

  # Staging/Production
  # ==================
  # host: 127.0.0.1
  # port: 3307
  # password: <%= ENV['LOGCHECK_RDS_PASSWORD'] %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql2
  encoding: utf8
  reconnect: false
  database: logcheck_test
  pool: 5
  username: logcheck
  password: logcheck
  socket: /opt/local/var/run/mysql55/mysqld.sock
